using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
//    public GameObject objectPrefab;
  //  public Transform parentObject;
    //private bool hasBeenInstantiated = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      /*  if (hasBeenInstantiated == false)
        {
            GameObject newObject = Instantiate(objectPrefab, parentObject);

            // Optionally, you can set the position and rotation of the new object relative to the parent
            newObject.transform.localPosition = Vector3.zero;
            newObject.transform.localRotation = Quaternion.identity;
            hasBeenInstantiated = true;
        }*/
    }
}
